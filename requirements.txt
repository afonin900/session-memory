# Core (ONNX path — fast, lightweight)
onnxruntime>=1.17
tokenizers>=0.15
numpy>=1.24
lancedb>=0.4
pyarrow>=14.0
fastmcp>=0.1

# Optional: PyTorch fallback (only if ONNX model not exported)
# pip install sentence-transformers torch
